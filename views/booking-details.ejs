<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Booking Details - Travel</title>

  <!-- FONT -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap" rel="stylesheet">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/styles/style.css" />
  <link rel="stylesheet" href="/styles/booking-details.css" />
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="booking-details-wrapper">
    <div class="container">
      <div class="booking-header">
        <h1><%= booking.packageName %></h1>
        <p class="destination"><%= booking.destination %></p>
      </div>

      <div class="booking-info-card">
        <div class="info-grid">
          <div>
            <strong>Date:</strong> <%= booking.date %>
          </div>
          <div>
            <strong>Travelers:</strong> <%= booking.people %>
          </div>
          <div>
            <strong>Total Price:</strong> Rs. <%= booking.totalPrice %>
          </div>
          <div>
            <strong>Booking ID:</strong> <%= booking.id %>
          </div>
        </div>

        <div class="booking-actions">
          <button class="btn-primary">Download Invoice</button>

          <form
          action="/booking/<%= booking.id %>/cancel"
          method="POST"
          style="display:inline;"
          onsubmit="return confirm('Are you sure you want to cancel this booking?');"
          >
          <button type="submit" class="btn-outline">Cancel Booking</button>
          </form>
        </div>


      <div class="booking-extra">
        <h2>About Your Trip</h2>
        <p>
          Your <%= booking.packageName %> adventure is confirmed! Get ready for an unforgettable experience.
          We'll send you all the necessary travel details and updates before your departure.
        </p>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
