<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= package.name %> - Travel</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap"
      rel="stylesheet"
    />

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/styles/style.css" />
    <link rel="stylesheet" href="/styles/package-details.css" />
  </head>

  <body>
    <%- include('partials/navbar') %>

    <div class="package-details-wrapper">
      <div
        class="package-hero"
        style="background-image: url('/images/carousel1.jpg')"
      >
        <div class="hero-overlay-dark"></div>

        <div class="hero-layout-container">
          <div class="booking-section">
            <h2>Book This Trip</h2>
            <form action="/payment" method="GET" class="booking-form">
              <label for="date">Select Date</label>
              <input type="date" id="date" name="date" required />

              <label for="people">Number of Travelers</label>
              <input
                type="number"
                id="people"
                name="travelers"
                min="1"
                value="1"
                required
              />

              <!-- Hidden input for package id -->
              <input
                type="hidden"
                name="package_id"
                value="<%= package.package_id %>"
              />

              <button type="submit" class="book-btn">Book Now</button>
            </form>
          </div>

          <div class="hero-text-content">
            <h1><%= package.package_name %></h1>
            <p><%= destinationList %></p>

            <p><%= package.description %></p>

            <div class="details-grid">
              <div>
                <strong>Duration:</strong> <%= package.duration_days %> Days
              </div>
              <div>
                <strong>Price:</strong> Rs. <%= package.price %> per person
              </div>
              <div><strong>Category:</strong> <%= package.theme %></div>
            </div>
          </div>
        </div>
      </div>

      <div class="package-content">
        <div class="container">
          <div class="transport-section">
            <h2>Available Transport Options</h2>

            <% if (transports.length > 0) { %>
            <div class="transport-grid">
              <% transports.forEach(t => { %>
              <div class="transport-card">
                <h4><%= t.mode %></h4>
                <p><strong>Company:</strong> <%= t.company %></p>
                <p>
                  <strong>Price:</strong> Rs. <%= t.price.toLocaleString() %>
                </p>
              </div>
              <% }) %>
            </div>
            <% } else { %>
            <p>No transport options available for this package.</p>
            <% } %>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
