<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Something Went Wrong</title>

    <!-- Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&family=Inter:wght@400;600&display=swap"
      rel="stylesheet"
    />

    <!-- Bootstrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles/style.css" />

    <style>
      html,
      body {
        margin: 0;
        height: 100%;
        font-family: "Inter", sans-serif;
        background: url("/images/error.webp") center/cover no-repeat fixed;
        color: #ffffff;
      }

      .error-container {
        min-height: calc(100vh - 80px);
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 3rem 1.5rem;
      }

      h1 {
        font-family: "Montserrat", sans-serif;
        font-size: clamp(3.4rem, 8vw, 3rem);
        font-weight: 600;
        margin-bottom: 1rem;
      }

      p {
        font-size: 1.25rem;
        margin-bottom: 2rem;
        opacity: 0.95;
      }

      .back-btn {
        background-color: #333333;
        font-family: "Open Sans", sans-serif;
        color: #ffffff;
        border: none;
        padding: 15px 40px;
        cursor: pointer;
        font-size: 0.9rem;
        font-weight: 600;
        letter-spacing: 1.5px;
        transition: all 0.3s ease; /* Changed to all properties with ease */
      }

      .back-btn:hover {
        background-color: var(--dark-green);
        transform: translateY(-2px); /* Added subtle lift effect */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Added subtle shadow */
      }
    </style>
  </head>

  <body>
    <%- include('partials/navbar') %>

    <div class="error-container">
      <div>
        <h1>INTERNAL SERVER ERROR</h1>
        <p>Something unexpected happened. We're working on fixing it.</p>
        <a href="/" class="back-btn">RETURN HOME</a>
      </div>
    </div>
  </body>
</html>
